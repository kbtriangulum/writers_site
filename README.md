# Door Site Static HTML Version

Plain HTML/CSS/JavaScript site for GitHub Pages.

Open `index.html` directly, or run a local server:

```bash
python3 -m http.server 8000
```

Then open http://localhost:8000
